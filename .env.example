# Binance shadow connector configuration
# IMPORTANT: never commit real values. Copy this file to .env locally.

BINANCE_API_KEY=
BINANCE_API_SECRET=

# Read-only shadow mode only. No order endpoint exists in the connector.
BINANCE_BASE_URL=https://api.binance.com
